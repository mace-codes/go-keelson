# go-keelson
Keelson — a Go service template implementing DDD-informed hexagonal architecture (ports &amp; adapters) with explicit bounded-context boundaries, anti-corruption layers, and dependency-injected composition root.
